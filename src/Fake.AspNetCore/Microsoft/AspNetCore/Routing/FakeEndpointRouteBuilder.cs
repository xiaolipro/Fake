namespace Microsoft.AspNetCore.Routing;

public static class FakeEndpointRouteBuilder
{
    // public static void MapEndpoints(this WebApplication webApplication)
    // {
    //     var mapper = webApplication.Services.GetService<IEndpointServiceMapper>();
    //     foreach (var service in webApplication.Services.GetServices<IEndpointService>())
    //     {
    //         var serviceInstance = (ServiceBase)serviceType;
    //         if (serviceInstance.RouteOptions.DisableAutoMapRoute ?? serviceMapOptions.DisableAutoMapRoute ?? false)
    //             continue;
    //
    //         serviceInstance.AutoMapRoute(serviceMapOptions, serviceMapOptions.Pluralization);
    //     }
    // }
}